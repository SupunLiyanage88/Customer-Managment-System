# Customer Management System

Small Spring Boot application compatible with Java 8.

## Run

```bash
mvn spring-boot:run
```

## Endpoint

- `GET /api/customers` returns a small in-memory customer list.
